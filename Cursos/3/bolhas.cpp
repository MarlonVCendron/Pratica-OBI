#include <bits/stdc++.h>
#include <iostream>
#define _ ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0)
using namespace std;

#define MAXN 100010

int n;
int seq[MAXN]

int main(){_;
  while (true) {
    cin >> n;
    if(n == 0) break;

    for (size_t i = 0; i < n; i++) {
      cin >> seq[i];
    }

    

  }
  return 0;
}
